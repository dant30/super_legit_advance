import React from "react";
import ReportPreview from "./ReportPreview";

const CollectionReport = ({ report }) => {
  return <ReportPreview title="Collection Report" report={report} />;
};

export default CollectionReport;
